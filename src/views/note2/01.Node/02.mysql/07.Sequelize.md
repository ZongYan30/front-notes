# ORM

Object Relational Mapping 对象关系映射,通过ORM框架，可以自动的把程序中的对象和数据库关联。

**特点**
ORM框架会隐藏具体的数据库底层细节，让开发者使用同样的数据操作接口，完成对不同数据库的操作

**优势**

- 开发者不用关心数据库，仅需关心对象
- 可轻易的完成数据库的移植
- 无须拼接复杂的sql语句即可完成精确查询

**node中的ORM**
几乎每一个语言都有对应的ORM框架,node中常见的有：

- Sequelize (JS TS 成熟)
- TypeORM (TS 不成熟)

**ORM原理图**
<img src="https://zongyan30.github.io/front-assets/Node/ORM原理图.jpg"/>

## sequelize使用

![sequelize中文文档](https://github.com/demopark/sequelize-docs-Zh-CN)

### 安装

```sh
# 安装ORM
npm install --save sequelize;
# 安装数据库驱动
npm install --save mysql2;
```

### 使用

新建models文件夹，存放数据库模型文件。
:::code-group

```js [db.js]
const { Sequelize } = require("sequelize");

const sequelize = new Sequelize("myschooldb", "root", "ybybdwyJ42.", {
  host: "localhost",
  dialect: "mysql",
  logging: null,
});

module.exports = sequelize;
```

```js [Admin.js]
const sequelize = require("./db");
const { DataTypes } = require("sequelize");
//创建一个模型对象
const Admin = sequelize.define(
  "Admin",
  {
    loginId: {
      type: DataTypes.STRING,
      allowNull: false,
    },
    loginPwd: {
      type: DataTypes.STRING,
      allowNull: false,
    },
  },
  {
    createdAt: false,
    updatedAt: false,
    paranoid: true, //从此以后，该表的数据不会真正的删除，而是增加一列deletedAt，记录删除的时间
  },
);

module.exports = Admin;
```

```js [Class.js]
const sequelize = require("./db");
const { DataTypes } = require("sequelize");
const Student = require("./Student.js");

// 创建一个模型对象
const Class = sequelize.define(
  "Class",
  {
    name: {
      type: DataTypes.STRING,
      allowNull: false,
    },
    openDate: {
      type: DataTypes.DATE,
    },
  },
  {
    createdAt: false,
    updatedAt: false,
    paranoid: true,
  },
);

/* 
A.hasOne(B); // A 有一个 B
A.belongsTo(B); // A 属于 B
A.hasMany(B); // A 有多个 B
A.belongsToMany(B, { through: 'C' }); // A 属于多个 B , 通过联结表 C
*/
Class.hasMany(Student); //表关联,一对多

module.exports = Class;
```

```js [sync.js]
// 同步所有模型
const sequelize = require("./db");

require("./Admin");
require("./Book");
require("./Class");
require("./Student");

sequelize.sync({ alter: true }).then(() => {
  console.log("所有模型同步完成");
});
```

:::

index.js

```js [index.js]
// 执行同步操作
require("./sync");
```
